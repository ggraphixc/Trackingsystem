{
  "name": "tracknaija-agent",
  "version": "0.2.0",
  "description": "TrackNaija desktop agent — installs on laptops/desktops to report Wi-Fi/IP location, capture webcam in lost mode, support remote lock/alarm, and sync offline evidence via the offline vault",
  "main": "src/main.js",
  "author": "TrackNaija",
  "license": "MIT",
  "scripts": {
    "icon": "node scripts/generate-icon.js",
    "start": "electron .",
    "dev": "electron . --dev",
    "dist": "electron-builder",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/tracking-engine.js && node --check src/commands.js && node --check src/ui.js && node --check src/offline-vault.js && node --check src/sync-client.js && node --check src/ble-scan.js && node --check scripts/generate-icon.js && node --check src/renderer/app.js"
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8"
  },
  "build": {
    "appId": "com.tracknaija.agent",
    "productName": "TrackNaija Agent",
    "files": ["src/**/*", "assets/**/*"],
    "win": { "target": ["nsis"] },
    "mac": { "target": ["dmg"], "category": "public.app-category.utilities" },
    "linux": { "target": ["AppImage"], "category": "Utility" }
  }
}
